import { css } from '@emotion/core'

// buttons
export const styledBNaviButton = css({
  minWidth: `80px`,
  width: `25%`,
  maxWidth: `200px`,
})

export const baseInnerWidth = css({
  width: `100%`,
  maxWidth: `960px`,
  margin: `auto`,
})
