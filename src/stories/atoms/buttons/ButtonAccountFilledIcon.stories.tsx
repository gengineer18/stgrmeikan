import * as React from 'react'
import { storiesOf } from '@storybook/react'
import { ButtonAccountFilled } from '@/components/atom'

storiesOf(`atoms/buttons`, module).add(`ButtonAccountFilled`, () => <ButtonAccountFilled />)
